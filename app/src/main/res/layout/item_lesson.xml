<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginTop="8dp"
    app:cardCornerRadius="0dp"
    app:cardElevation="2dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Номер урока -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvLessonNumber"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="@drawable/circle_background"
            android:gravity="center"

            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="1" />

        <!-- Название предмета -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvSubjectName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"

            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@+id/cardMark"
            app:layout_constraintStart_toEndOf="@id/tvLessonNumber"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Математика" />

        <!-- Время и кабинет -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvLessonInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="#888888"
            android:textSize="12sp"
            app:layout_constraintStart_toStartOf="@id/tvSubjectName"
            app:layout_constraintTop_toBottomOf="@id/tvSubjectName"
            tools:text="08:00 - 08:40 | каб. 305" />

        <!-- Домашнее задание -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvAssignment"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:maxLines="2"

            android:textSize="14sp"
            app:layout_constraintEnd_toStartOf="@+id/cardMark"
            app:layout_constraintStart_toStartOf="@id/tvSubjectName"
            app:layout_constraintTop_toBottomOf="@id/tvSubjectName"
            tools:text="Решить задачи на странице 45" />

        <!-- Оценка (в отдельной карточке для красоты) -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cardMark"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:cardBackgroundColor="?attr/colorSurface"
            app:cardCornerRadius="0dp"
            app:cardElevation="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <androidx.appcompat.widget.AppCompatTextView
                android:id="@+id/tvMark"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"

                android:textSize="20sp"
                android:textStyle="bold"
                tools:text="5" />
        </androidx.cardview.widget.CardView>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>